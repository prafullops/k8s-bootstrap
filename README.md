# Project Overview
This repository host file and configuration to bootstrap kubernetes cluster in local.

# pre-requisites

### Install Kind
follow below steps in powershell to install kind version v0.32.0