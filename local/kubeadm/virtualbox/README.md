# Installing Kubernetes the kubeadm way on  VirtualBox

decide which netwok type to choose for vms. options are NAT(virtualbox will give ip and access to internet), Bridged(wifi provider will give), Host-only(virtual box will give, but this will not allow node to node communication.) and why




